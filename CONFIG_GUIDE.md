# 遊戲配置說明

本遊戲使用**靜態 JSON 檔案**進行配置，無需資料庫或後端。

## 📁 配置檔案位置

```
public/game-config.json
```

## 📝 JSON 格式說明

### 完整範例

```json
{
  "game": {
    "title": "聖誕派對遊戲 2025",
    "sound": {
      "enabled": true,
      "volume": 0.7
    },
    "colorblindMode": false
  },
  "players": [
    {
      "id": "player-1",
      "name": "小明",
      "avatar": "player-1.jpg",
      "color": "#ef4444"
    }
  ],
  "stages": [
    {
      "id": "stage-1",
      "name": "幸運盤面",
      "type": "LuckBoard",
      "config": {
        "rows": 10,
        "cols": 6,
        "greenCount": 30,
        "redCount": 30,
        "greenScore": 10,
        "redScore": -5
      }
    }
  ]
}
```

## 👥 玩家設定 (players)

### 必填欄位

| 欄位 | 類型 | 說明 | 範例 |
|------|------|------|------|
| `id` | string | 唯一識別碼 | `"player-1"` |
| `name` | string | 玩家名稱 | `"小明"` |
| `avatar` | string | 頭像（emoji 或圖片檔名） | `"🎅"` 或 `"player-1.jpg"` |
| `color` | string | 代表顏色（16進制） | `"#ef4444"` |

### 頭像 (avatar) 設定方式

#### 方式 1：使用 Emoji
```json
{
  "id": "player-1",
  "name": "小明",
  "avatar": "🎅",
  "color": "#ef4444"
}
```

#### 方式 2：使用圖片
```json
{
  "id": "player-1",
  "name": "小明",
  "avatar": "player-1.jpg",
  "color": "#ef4444"
}
```

圖片需放在：`public/images/players/player-1.jpg`

### 常用顏色代碼

```json
{
  "聖誕紅": "#ef4444",
  "翠綠": "#10b981",
  "金色": "#f59e0b",
  "冰藍": "#3b82f6",
  "紫色": "#8b5cf6",
  "粉紅": "#ec4899",
  "橘色": "#f97316",
  "青色": "#06b6d4"
}
```

## 🎮 關卡設定 (stages)

### 幸運盤面 (LuckBoard)

```json
{
  "id": "stage-1",
  "name": "幸運盤面",
  "type": "LuckBoard",
  "config": {
    "rows": 10,
    "cols": 6,
    "greenCount": 30,
    "redCount": 30,
    "greenScore": 10,
    "redScore": -5
  }
}
```

| 欄位 | 說明 | 建議值 |
|------|------|--------|
| `rows` | 列數 | 10 |
| `cols` | 欄數 | 6 |
| `greenCount` | 綠格子數量（得分） | 30 |
| `redCount` | 紅格子數量（扣分） | 30 |
| `greenScore` | 抽到綠格得分 | 10 |
| `redScore` | 抽到紅格扣分 | -5 |

### 熟人問答 (Quiz)

```json
{
  "id": "stage-2",
  "name": "熟人問答",
  "type": "Quiz",
  "config": {
    "questions": []
  }
}
```

（待開發）

### 你比我猜 (Guess)

```json
{
  "id": "stage-3",
  "name": "你比我猜",
  "type": "Guess",
  "config": {
    "topics": []
  }
}
```

（待開發）

## 🔊 遊戲設定 (game)

### 音效設定

```json
{
  "sound": {
    "enabled": true,   // 是否啟用音效
    "volume": 0.7      // 音量 (0.0 - 1.0)
  }
}
```

### 無障礙設定

```json
{
  "colorblindMode": false  // 色弱模式（增加圖案辨識）
}
```

## 📸 玩家圖片設定

### 1. 準備圖片

- 格式：`.jpg`、`.png`、`.webp`
- 尺寸：建議 **300x300 像素**（正方形）
- 大小：< 500KB

### 2. 放置圖片

將圖片放入：
```
public/
└── images/
    └── players/
        ├── player-1.jpg
        ├── player-2.jpg
        └── default.jpg  ← 預設圖片（找不到時使用）
```

### 3. 更新 JSON

```json
{
  "players": [
    {
      "id": "player-1",
      "name": "小明",
      "avatar": "player-1.jpg",  ← 對應到 public/images/players/player-1.jpg
      "color": "#ef4444"
    }
  ]
}
```

## ⚡ 快速開始

### 1. 編輯玩家資料

編輯 `public/game-config.json`：

```json
{
  "players": [
    {
      "id": "player-1",
      "name": "你的名字",
      "avatar": "🎅",
      "color": "#ef4444"
    },
    {
      "id": "player-2",
      "name": "朋友名字",
      "avatar": "🎄",
      "color": "#10b981"
    }
  ]
}
```

### 2. 重新載入頁面

儲存檔案後，刷新瀏覽器即可看到新的設定。

### 3. （可選）新增玩家圖片

如果想使用真實照片：
1. 將照片改名為 `player-1.jpg`
2. 放入 `public/images/players/`
3. 修改 JSON 的 `avatar` 欄位為 `"player-1.jpg"`

## 🚀 部署

### 建置靜態網站

```bash
npm run generate
```

輸出檔案在 `.output/public/`

### 確認檔案

確保這些檔案都存在：
```
.output/public/
├── game-config.json        ← 遊戲配置
└── images/
    └── players/
        ├── player-1.jpg    ← 玩家圖片
        └── default.jpg     ← 預設圖片
```

## ❓ 常見問題

### Q: 修改 JSON 後沒有變化？

A: 請按 **Ctrl+F5** (Windows) 或 **Cmd+Shift+R** (Mac) 強制刷新。

### Q: 圖片沒有顯示？

A: 檢查：
1. 檔名是否與 JSON 中的 `avatar` 一致
2. 圖片是否放在 `public/images/players/` 資料夾
3. 瀏覽器開發者工具 Console 是否有錯誤

### Q: 可以中途更換玩家嗎？

A: 可以！編輯 `game-config.json` 後重新整理頁面即可。但**分數會重置**，因為玩家 ID 改變了。

### Q: 如何重置遊戲？

A: 在首頁點擊「重置遊戲」，或清除瀏覽器的 LocalStorage。

## 📚 相關文檔

- [Nuxt 4 專案結構](./NUXT4_STRUCTURE.md)
- [組件命名規則](./NUXT4_COMPONENTS.md)
- [幸運盤面測試](./LUCK_BOARD_TEST.md)
