# 玩家圖片說明

## 📁 目錄結構

```
public/
├── images/
│   └── players/
│       ├── player-1.jpg    # 小明的照片
│       ├── player-2.jpg    # 小華的照片
│       ├── player-3.jpg    # 小美的照片
│       ├── player-4.jpg    # 小強的照片
│       ├── player-5.jpg    # 小芳的照片
│       └── default.jpg     # 預設圖片（找不到玩家圖片時使用）
└── game-config.json        # 遊戲設定檔
```

## 📝 使用說明

1. 將玩家照片放入 `public/images/players/` 資料夾
2. 檔名必須與 `game-config.json` 中的 `avatar` 欄位一致
3. 支援格式：`.jpg`、`.png`、`.webp`
4. 建議尺寸：300x300 像素（正方形）
5. 如果找不到對應圖片，會自動使用 `default.jpg`

## 🎨 範例

在 `game-config.json` 中：
```json
{
  "id": "player-1",
  "name": "小明",
  "avatar": "player-1.jpg",  // ← 對應到 public/images/players/player-1.jpg
  "color": "#ef4444"
}
```

或使用 emoji（不需要圖片檔案）：
```json
{
  "id": "player-1",
  "name": "小明",
  "avatar": "🎅",  // ← 直接使用 emoji，不需要圖片
  "color": "#ef4444"
}
```

## ⚠️ 注意事項

- 圖片檔案不要太大（建議 < 500KB）
- 使用 `.webp` 格式可以獲得更好的壓縮效果
- 圖片會自動裁切成圓形顯示
